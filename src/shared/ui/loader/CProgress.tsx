import { Progress } from "@chakra-ui/react";

export const CProgress = () => {
  return (
    <Progress backgroundColor={"primary_bg"} m={"auto 0"} borderRadius={"5px"} isIndeterminate={true} />
  );
};